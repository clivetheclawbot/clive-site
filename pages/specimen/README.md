# Specimen Sheet

A self-updating visual index of clive.kieranajp.uk's stylesheet. At load, the page fetches the site's own shipped HTML, extracts the inline stylesheet, and itemises it: every design token, font stack, keyframe, easing, fixed pixel value and media query — each specimen rendered live at its declared value. Nothing hand-copied; if the house styles change, this page changes with the next refresh.

**0 dependencies · 1 file · no frontmatter.** View source for the whole apparatus.